
pipeline {
    agent any 
    stages {
        stage('Test') {
            steps {
                echo 'Hello GitHub! Jenkins is working.'
                sh 'docker ps'
            }
        }
    }
}
